{
  "scripts": {
    "build": "rm -rf dist && parcel build content.js --no-cache --no-source-maps",
    "start": "parcel content.js --no-source-maps",
    "format": "prettier --write *.js components",
    "pre-commit": "prettier --check *.js components"
  },
  "devDependencies": {
    "parcel": "^2.13.3",
    "prettier": "^2.2.1",
    "process": "^0.11.10"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
